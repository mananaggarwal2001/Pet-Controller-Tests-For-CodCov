package com.mananluvtocode.pet_clinic;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit.jupiter.SpringExtension;

// this is the extension of the JUNIT 5 test class for doing the testing of the applications.
// if the JUnit 4 excluded and the modern the Junit 5 is used for testing purpose.
@ExtendWith(SpringExtension.class)
@SpringBootTest
class PetClinicApplicationTests {

	@Test
	void contextLoads() {

	}
}
